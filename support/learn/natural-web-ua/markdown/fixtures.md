# Relative URL rewriting test

This markdown is loaded via a `src` attribute, so relative URLs should be rewritten back to the directory containing this file.

![Relative example image](./assets/example.png)

[Relative link target](./assets/relative-link-target.html)
